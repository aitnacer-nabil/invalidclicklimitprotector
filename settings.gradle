pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven {
            //IronSource
            url "https://android-sdk.is.com/"
        }
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            //IronSource
            url "https://android-sdk.is.com/"
        }
    }
}

rootProject.name = "Invalid Click & Limit Protector"

include ':app'
include ':library'
